# Application
spring:
  main:
    web-application-type: none

selenium:
  headlessBrowser: false                       # false/true(to run chrome in background)
  targetUrl: https://localhost:3000/login
  browser: chrome                       #chrome or firefox
  remoteBrowser: normal
